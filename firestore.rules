
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Private software: Access is controlled by application-level logic
    // We allow read/write because the user requested to remove Firebase Authenticator
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
